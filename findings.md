# Findings

## 参照実物（`~/Projects/google-sheets-tournament-import/output/`）
- `8team_volleyball_base.xlsx`: 完全順位決定・8チーム。進行表とトーナメント表の2タブ。
  条件付き書式もスマホ用タブも無く、レイアウトの参照にはなるが自動進行の参照にはならない。
- `10team_double_auto.xlsx`: ダブルエリミ・10チーム・4コート。4タブ。
  条件付き書式101件（38/57/6）、試合管理153数式。**完成形の全機能を含む唯一の参照。**
- `10team_double_base.xlsx`: 上記の変換元（自動進行なし）。

## 構造に関する知見
- 完全順位決定は再帰的な二分割で表せる。2^m チームを m 回戦で完全順位づけする手続きは、
  1回戦で二分し、勝者側と敗者側それぞれを再帰的に順位づけるだけ。
  総試合数 N*log2(N)/2、各チームちょうど log2(N) 試合。
- 試合番号は「ラウンド優先、同一ラウンド内は下位の順位帯を先」に振ると実物と一致する。
  8チームで ⑤⑥=下山、⑦⑧=上山、⑨=7位決定 ⑩=5位決定 ⑪=3位決定 ⑫=決勝 になる。
- この形式は N=2^k が絶対条件。シードを1つ入れると試合数が不均等になり、
  「全員同じ試合数・全順位確定」という形式の価値そのものが失われる。
- コート1面のときの枠割当は、依存関係だけを見る素朴な貪欲法で実物の進行順①〜⑫を再現する。

## レイアウトに関する知見
- 8チーム版のトーナメント表は「2列で1ラウンド、行間隔が倍々」の規則的配置。
  列A(幅7)=試合番号 / B(20)=左チーム / C(5)=vs・連結線 / D(20)=右チーム / E(5)=空 / F(26)=説明。結合セル33個。
- 10チーム版も同じ規則（B8/B12 → D10、列Cは罫線用）。座標は閉じた式で計算できる。
- 既存の「見やすい版」の読みやすさは、A4に収めるため人間が不均等に詰めた結果であり、
  汎用生成とは両立しない。標準レイアウトは既存より素朴になる。

## 技術的な制約
- Apps Script の V8 ランタイムは `import`/`export` を解釈しない。
  core を両方から使うには esbuild で単一ファイルへバンドルし、成果物をコミットする必要がある。
- 条件付き書式の一括生成には `spreadsheets.batchUpdate` が要る。
  core が batchUpdate のリクエスト配列を出す設計にすれば、
  Apps Script（Advanced Sheets Service）・Node CLI（googleapis）・開発時（MCP）の3経路で同じ payload を使える。
- **未検証（Phase 5の最優先項目）**: Advanced Sheets Service を使うスクリプトが、
  Sheetsのコピーで後輩の環境へ引き継がれるか。継承の可否に直結する。
- Node 26 は `node --test test/` をモジュールパスとして解釈する。glob を明示する必要がある。

## 運用に関する知見
- 既存表には「1試合＝6人ダブルス3本、結果は 3-0/2-1/1-2/0-3 の4択」という競技固有の
  スコアモデルが埋め込まれている。汎用化には抽象化が必要で、勝者判定さえできれば全形式が回る。
- Sheets標準機能では2箇所の入力欄を双方向同期できない。入力口は1箇所に固定する必要がある。
- テンプレSheetsは harupan119 の個人Drive（119tempestkh@gmail.com）に置く。
  **卒業してアカウントを整理すると後輩がアクセスを失う**ため、READMEに明記が必要。

## 実適用で判明した罠
- **結果セルには `numberFormat: TEXT` が必須。** USER_ENTERED で `2-1` や `1-2` を書くと
  Google Sheets が日付として解釈し、日付シリアル（46054 / 46024）になる。
  勝敗判定の文字列比較が黙って外れるが、`3-0` / `0-3` は不正な日付なので文字列のまま残るため、
  12試合中3試合だけ勝者が反転するというまだらな症状になり、目視では原因に辿り着きにくい。
  さらに `1-2` の場合は「日付化けで右に落ちる」結果と「本来右が勝つ」結果が一致して症状が隠れる。
- この種のバグはローカルのユニットテストでは原理的に検出できない。
  Sheets に実際に適用して読み戻す工程が要る。
