# 進捗記録

## 2026-08-28
- /grilling を5ラウンド実施し、Q1〜Q30の設計判断をすべて確定。ユーザーの着手許可を取得。
- 既存3プロジェクト（xlsx-tournament-repair / xlsx-volleyball-2026 / google-sheets-tournament-import）を調査。
  いずれも「既にある表を直す」方向で、今回の「ゼロから生成する」方向とは別物と確認。
- `findings.md` に残っていたIDでDriveから3件をローカル取得。`unzip -t` 全件 No errors detected。
  条件付き書式はトーナメント表38・タイムテーブル57・スマホ用6で、記録と一致。
  XLSXエクスポートを経由しても条件付き書式が保持されることを確認した。
- 8チーム版の構造解析で、Q3の「予選決勝順位決定」の正体が完全順位決定トーナメントだと判明。
  8チーム・全12試合・各チーム3試合・1位〜8位まで確定。
- 完全順位決定は N=2^k でないと成立しないため、4/8/16のみ許可しそれ以外はエラーで拒否する方針を確定。
- ダブル版の解析で「枠」の概念、シード実装（A・Bが枠2から登場）、決勝Rの条件付き実施、
  入力口が `スマホ用!C5:C23` に一本化されていることを確認。
- 既存表に「1試合＝6人ダブルス3本」という競技固有のスコアモデルが埋め込まれていたため、
  スコアをプリセット化する方針を追加（既定は勝敗のみ、既存の4択は sets-of-3）。
- MVPを形式3・8チームに決定。シードがなく座標規則を素の形で検証でき、
  全勝敗パターン4096通りの総当たり検証が現実的な計算量に収まるため。
- Phase 1開始。リポジトリ骨格・バリデーション・構造生成・枠割当・テストを実装。
- 生成した8チームの依存関係が、実物 `進行表` G13:G24 の記述と完全一致することを確認。
  ①→勝者⑦/敗者⑤ 〜 ⑫決勝、⑨=7位決定 ⑩=5位決定 ⑪=3位決定。
- ラウンド名の初版は「準決勝（5〜8位）」だったが、実物の「2回戦(下山)」「準決勝(上山)」へ寄せた。
  2回戦だけが上下半分の分割になるため、3回戦以降は順位帯表記にする規則で一般化した。
- `npm test` の初回は Node 26 が `--test test/` をモジュールパスと解釈して MODULE_NOT_FOUND。
  `node --test test/*.test.js` へ変更して解消。
- `npm test`: 8件すべてPASS。4096通りの全勝敗パターンで順位1〜8位が重複も欠けもなく確定し、
  各チームの試合数が常に3であることを確認。コート1面の枠割当が実物の進行順①〜⑫と一致。
- Phase 1完了。
- Phase 2開始。座標規則を実物から導出: 行 = base + 2^(j+1)*i + 2^j、列 = 2 + 2j。
  base=12 のとき B13/B15/B17/B19 → D14/D18 → F16 となり、実物の配置と一致することをテストで固定。
- `Grid` にセル二重書き込み・結合範囲の重なり・結合範囲内側の値の3種の衝突検出を実装。
- トーナメント表の構成を実物と同じ4部構成にした。タイトル / 最終2ラウンドより前のラウンドを試合リスト /
  終端グループ（4チーム）を標準ブラケット図 / 最終順位表。4・8・16チームすべてで列がA〜Fに収まる。
- 初回レンダリングで3点の不備を検出。最終順位が試合順のまま（7,8,5,6,3,4,1,2）、
  ブラケットが下位帯から並ぶ、ブラケット見出しに試合番号がない。3点とも修正し、回帰テストを追加。
- `npm test`: 17件すべてPASS。
- Phase 2完了。Phase 3（Sheets生成ペイロード）はDriveへの書き込みを伴うため、着手前に承認を取る。
- Phase 3開始。google-multi MCP が切断されたため、Driveへの適用を伴わない範囲（数式とペイロード生成）を先に進める。
- scoring を3プリセット（win-loss / sets-of-3 / sets-of-5）に確定。
  Q25で挙げた `free`（自由記入）は勝者を機械判定できず、Q5で決めた自動進行が成立しないため落とした。
- 試合管理・スマホ用・進行表の3タブを実装。数式の形は既存の実物と同じ
  （`=IF('進行表'!$B$6="","（Aチーム）",'進行表'!$B$6)` / `=IF($D2="","",IF(OR($D2="3-0",$D2="2-1"),$B2,$C2))`）。
- 検証方法として、生成した数式から `$E$n` / `$F$n` を逆算して依存グラフを再構成し、
  モデルの left/right と突き合わせる方式を採った。数式評価器を作らずに配線の正しさを確認できる。
- テストが実バグを検出。`buildTournament` が未対応の `scoring` を素通りさせ、
  レイアウト生成まで遅れて落ちていた。生成の入口で `getScoring` を呼ぶよう修正。
- 「同じ枠に同じチームが2回出ない」を不変条件としてテストに固定した。
  依存関係から構造的に保証されるが、ダブルエリミ追加時に壊れうるため。
- `npm test`: 26件すべてPASS。
- `core/payload.js` を実装。グリッドを `spreadsheets.create` の body と `batchUpdate` の
  リクエスト配列へ変換する。生のリクエストデータなので Apps Script / Node CLI / MCP の3経路で同じものを使える。
- 8チーム2コートで 32リクエスト（updateCells 4 / updateDimensionProperties 16 / setDataValidation 12）。
- payload のテストを追加: 全リクエストが実在シートIDを指す、書き込み範囲が rowCount/columnCount に収まる、
  数式は formulaValue・文字列は stringValue、入力規則が全試合ぶんスマホ用タブに出る、入力セルに未入力色が付く。
- `npm test`: 33件すべてPASS。
- 残る未実施は「実際にDriveへ適用して読み戻す」検証のみ。google-multi MCP の再接続待ち。
- google-multi MCP が復帰。検証用スプレッドシートを作成し、
  試合管理・スマホ用の155セルを USER_ENTERED で書き込み。
- 未入力状態の読み戻しは正常（1回戦は仮チーム名、以降は空欄、全行「未入力」、数式エラー0）。
- 全12試合を投入したところ、②（C vs D、結果 2-1）の勝者が D になる不具合を検出。
  UNFORMATTED_VALUE で読むと `1-2` → 46024、`2-1` → 46054 の日付シリアルだった。
  `3-0` / `0-3` は不正な日付なので文字列のまま残り、症状がまだらに出ていた。
  ①③⑨は「日付化けで右に落ちる」結果と「本来右が勝つ」結果が偶然一致し、正しく見えていた。
- 原因は結果セルに TEXT 書式を指定していなかったこと。前回プロジェクトの findings.md が
  「D10の 2/1 が日付扱い」として記録していた罠と同じ。`core/payload.js` で
  `numberFormat: { type: 'TEXT' }` を付与するよう修正し、回帰テストを追加。
  **ローカルテストでは原理的に検出できず、実シートへの適用でのみ露見した種類のバグ。**
- 実シートにも TEXT 書式を適用し、値を消して再投入。全12試合の勝者がローカル検証器と完全一致。
- 読み返しでもう1点の抜けを検出。最終順位表のチーム名列が空、ブラケット図も静的テキストで
  自動進行に連動していなかった。`liveRefFormula` / `controlCell` を `core/sheets.js` の共有ヘルパにし、
  トーナメント表のブラケットセルと順位表を試合管理参照の数式へ置換。
- トーナメント表252セルを書き込み、読み戻しで最終順位 1位F/2位B/3位C/4位G/5位H/6位D/7位E/8位A を確認。
  ローカル期待値と完全一致。ブラケット図も上山 B,C,F,G→B,F→★F、下山 A,D,E,H→D,H→★H で正しく解決。
- `npm test`: 37件すべてPASS。
- Phase 3完了。検証シートには進行表の進行順セクションを未書き込み（検証目的には不要のため）。
- Phase 4開始。セルに `winnerOf` / `championOf` の役割タグを付け、そこから条件付き書式を起こす方式にした。
  8チームで winnerOf 24・championOf 3 のタグが付く。
- `championOf` が1つしか付かない不具合。置換文字列のエスケープ不一致で1行が当たっていなかった。行指定で修正。
- 30ルール（ブラケット27／スマホ用2／進行表1）を生成し実シートへ適用したところ、
  `Invalid ConditionValue.userEnteredValue` で全拒否。
  **Google Sheets は条件付き書式の数式に他シート参照を許さない**という制約を確認。
- INDIRECT による回避は、前回プロジェクトが再計算遅延で色が残る問題を踏んで捨てた経路なので採らず、
  トーナメント表内に隠し補助列 H（勝者）/ I（敗者）を置き、条件付き書式は `$H$n` を見る形へ変更。
  補助列は `hiddenByUser` で非表示にする。
- この制約をテストに落とし込んだ。CUSTOM_FORMULA は `!` を含まない・`INDIRECT` を含まない・
  補助列 `$[HI]$n` を参照する、の3点を全ルールで検査する。
- 補助列26セルと30ルール＋列非表示の計31リクエストを適用。全て成功。
- `effectiveFormat` で実測: B13(B,⑦勝者)=赤、B15(C,⑦敗者)=白、D14(B,⑫敗者)=白、
  B17(F,⑧勝者)=赤、D18(F,⑫勝者)=赤、F16(★F)=赤、B35(1位F)=赤、B36(2位B)=白。
  優勝チームFの経路だけが赤くつながることを確認。
- `npm test`: 43件すべてPASS。
- Phase 4完了。
- Phase 6開始。実物 `10team_double_auto.xlsx` の試合管理タブから依存関係を抽出。
  勝者側は 表①〜④(C-D/E-F/G-H/I-J) → 表⑤(W表②vsA) 表⑧(W表④vsB) → 表⑦表⑥ → 表⑨ という形で、
  **A・Bだけがシードの手作り独自形状**だった。標準の16枠シードブラケット（10チームならシード6・1回戦2試合）とは異なる。
  1例から一般則を逆算すると他のチーム数で破綻するため、**標準シードブラケットで実装し実物の形状は再現しない**方針を採った。
  試合数（勝者側9・敗者側8）という構造不変量は標準と一致するので、そこを検証の軸にする。
- 敗者側は「小ラウンド（敗者同士）→ 大ラウンド（勝者側の脱落者と対戦）」の交互構成で実装。
  シードは BYE 標識として扱い、試合生成時に BYE を含む対戦を素通りさせて畳む。
- 試合数が 4/5/8/10/12/16/20 チームすべてで 勝者側N-1・敗者側N-2・総数2N-1 に一致。
  10チームは 9/8/19 で実物と完全一致。
- 検証の軸として「優勝チーム以外は必ずちょうど2敗して終わる」という不変量を採用。
  ダブルエリミの定義そのものなので、ブラケット形状に依らず成立する。
- 決勝リセットを `playedIf: { match, side }` を持つ条件付き試合として模型化。
  シミュレータは条件を満たさない試合を飛ばし、決勝Rが実施された場合だけ1位・2位を上書きする。
- シート層に2つの未対応があり修正。(1) 決勝Rの対戦カードを条件式で包む
  （実物と同じ `=IF(AND($E$19<>"",$E$19=$C$19),$B$19,"")` の形）。
  (2) 勝者側と敗者側でラウンド番号が衝突するため、リストの区切りキーに系統を含めた。
- 最終順位は、同一順位を決める試合が複数ある場合に後の試合を優先する連結式にした。
  1位 = `IF(決勝Rの勝者<>"", 決勝Rの勝者, IF(決勝の勝者<>"", 決勝の勝者, ""))`。
- `npm test`: 51件すべてPASS。
- ダブルエリミの実シート検証は、Google Sheets API が `The service is currently unavailable` を2回連続で返したため未実施。
  リトライ可のエラーだが2回とも1分超で失敗したので中断し、APIの回復待ちとする。
- 並行実行された別エージェントが同リポジトリに重複実装を作っていたことを検出。
  `core/formats/double.js`・`test/double.test.js`（未追跡、テスト1件失敗）と、`core/index.js` への未使用import。
  重複実装はディスク上に温存し、削除の承認待ち。ただし未追跡ファイルへのimportは
  cloneしたときにモジュール読み込みが落ちるため、その1行だけ除去した。
- Phase 7開始。`seedOrder` を `core/util.js` へ移してシングルとダブルで共有。
- シングルエリミを実装。構造はダブルの勝者側と同じ標準シードブラケット（N-1試合）。
  3位決定戦は準決勝の敗者同士で、既定で有効。無効にすると3位が2チーム出るため。
  3位決定戦には丸数字を振らず、他の試合番号を詰める。
- 検証の軸は「優勝は無敗、それ以外はちょうど1敗」。シングルエリミの定義そのもの。
- `node --test`（フォークの重複テストを除く7ファイル）: 58件すべてPASS。
- Phase 7完了。3形式すべての構造生成とシート生成が揃った。
- Google Sheets API が回復。ダブルエリミ10チームの検証シートを作成し、試合管理150セル・スマホ用80セルを書き込み。
  スマホ用 C5:C23 に TEXT 書式を先に適用（Phase 3 の日付化け対策）。
- 決勝で敗者側代表が勝ち、決勝リセットが発動する分岐（bits=489905、期待 1位I/2位C/3位D）で検証。
- **3つ目の実バグを検出。** 決勝Rの勝者がローカル検証器（I）と実シート（C）で食い違った。
  原因は左右の割り当て規約の不一致。モデルは `決勝R = (決勝の勝者, 決勝の敗者)` だが、
  `core/sheets.js` は実物の慣習を写して `$B$19`/`$C$19`（決勝の当初の左右）を出していた。
  同じ `1-2` が逆の勝者を指す。`$E$19`（勝者）/`$F$19`（敗者）へ修正。
- 既存の依存グラフ逆算テストがこれを見逃していた。条件式内の `$E$19` を先に拾い、
  実際に返す値 `$B$19` を検査していなかったため。条件付き試合を専用テストに分離し、
  数式末尾の返り値を検査する形にした。あわせて逆算テストを3形式すべてに拡張。
- 修正後、実シートで 決勝R が `左=C 右=I` となり `1-2` で勝者I。ローカル期待値と一致。
- トーナメント表の順位表を書き込み、読み戻しで 1位I / 2位C / 3位D を確認。
  決勝Rを優先する連結式が実シート上で機能している。
- `node --test`（フォークの重複テストを除く7ファイル）: 59件すべてPASS。
- Phase 6完了。
- 重複ファイルの削除承認を取得し `rm core/formats/double.js test/double.test.js` を実行。作業ツリーがクリーンになり `npm test` が59件全通過。
- Phase 5の第一歩として、配布方式の生死を分ける一点だけを切り出したプローブを作成。
  `apps-script/probe.gs` と `apps-script/appsscript.json`（Advanced Sheets Service をマニフェストで有効化）。
  結果はセルZ1に `ADVANCED_OK` / `ADVANCED_NG` として残るので、APIで読めば判定できる。
  ユーザーのブラウザ操作待ち。
- esbuild で core を `apps-script/core.bundle.gs`（43KB）へバンドル。
  初回はバナー文字列のバックティックを zsh がコマンド置換として実行し、
  `npm run build` が再帰的に走ってバナーに npm の出力が埋め込まれた。バックティックを除去して解決。
- バンドルを Apps Script と同条件（bare な vm コンテキスト）で読み込むテストを追加。
  import/export の残存なし、`BracketGen` グローバルの定義、3形式の試合数とラベルがソースと一致、
  バリデーションの動作、`core/` の変更への追随を検査する。
  realm 違いで `deepEqual` が prototype 不一致になるため、JSON経由でこちら側へ写してから比較する。
- README（継承リスク・3形式の制約・実適用でしか見つからない不具合3件を明記）と LICENSE(MIT) を作成。
- `npm test`: 64件すべてPASS。
- Advanced Sheets Service の継承を実測。元シートに加え、Drive API でコピーを作りZ1を事前にクリアしてから
  ユーザーにメニュー実行してもらったところ、コピー側にも新しいタイムスタンプで `ADVANCED_OK` が入った。
  **マニフェスト経由なら継承される。** Q14/Q30の配布方式が成立することを確認。
- Phase 5本体を実装。`apps-script/main.gs`（メニュー・生成・sheetId の差し替え）と
  `apps-script/dialog.html`（形式・チーム数・コート数・大会名・スコア方式・オプションの入力）。
  生成は「いま開いているコピー」へ直接書き込む。作り直しに備えて、
  既存タブの内容・条件付き書式・結合セルを明示的に消してから適用する。
- ダイアログの選択肢は `BracketGen.SCORING` から動的に作る（二重管理を避けるため）。
- Apps Script 側の検証テストを追加。構文検査、マニフェストのAdvanced Sheets Service宣言、
  **main.gs が参照する `BracketGen.*` がバンドルに実在するか**、`remapSheetIds` の再帰的な差し替え、
  ダイアログが送る options のキーが core の受け口と一致するか。
- READMEのYAML例が `third_place`、実装が `thirdPlace` で食い違っていたため実装側に統一。
  この種の事故を防ぐため、ダイアログのキーをテストで固定した。
- `npm test`: 69件すべてPASS。

## 2026-08-29
- ユーザーがブラウザから初回生成を実施（シングル8チーム2コート、既定値）。生成の配管は正常に動作。
  4タブ生成、試合管理は非表示、条件付き書式はトーナメント表17・進行表1・スマホ用2で
  **前回のダブルエリミ分は累積せず消えていた**。結合セルの残骸もなし。
- ただしレイアウトに不備3件。ユーザーの「トーナメント表とかできてない」はこれ。
  (1) 枝分かれの図が出ず試合リストだけ (2) サブタイトルの「各チーム3試合」が誤り
  (3) 見出し「■ 3位決定戦（3位決定戦〜⑦）」が壊れている。
- 原因は、ブラケット描画が完全順位決定の構造（最終2ラウンドの4チームグループ）に固定されていたこと。
  シングル／ダブルでは該当グループが空になり、全部リストに落ちていた。
- 構造側にスロット列 `tree.levels` を持たせるよう変更。不戦勝の枠は null のまま残すので
  常に 2^k 個の規則的な構造になり、座標を閉じた式で計算できる。枠を詰めると式が壊れる。
- `renderTree` を追加し、任意のシード付きブラケットを図として描けるよう一般化。
  ダブルは勝者側を図・敗者側をリストにする（敗者側は木構造にならないため）。
- 枝を結ぶ罫線が無いと「表」に見えないため `drawBranches` を追加。
  チーム名セルの下線が横線、連結列の左罫線が縦線。実物と同じ組み方。
  完全順位決定のグループ描画にも同じ関数を適用し、3形式すべてで枝が引かれるようにした。
- 補助列がブラケットの列と衝突する（8チームでH列）ため、`helperCols` でブラケット右端の外へ動的に配置。
- 罫線の幾何をテストで固定（`B6:B6/bottom`、`C7:C8/left`、`E8:E11/left`、`H13:H13/bottom` 等）。
- `npm test`: 72件すべてPASS。バンドルを再生成（45.8KB）。
- 修正版バンドルで再生成され、ブラケット図と枝の罫線が実シートに出ることを確認。
  B列に8チーム、連結列に縦線、各セルに下線。D列は①〜④の勝者を表示。
- ユーザーが結果を入力して動作を確認。スマホ用に「左の勝ち／右の勝ち」を8試合ぶん入力すると、
  試合管理で勝者・敗者・状態が確定し、トーナメント表のD列以降に実チーム名が反映された。
  自動進行は意図どおり働いている。
- **進行表の説明文だけ「各チーム3試合」の誤りが残っていた**（トーナメント表側だけ直していた）。
  シングル／ダブルでは試合数が一定にならないので誤り。`eliminationRule` を共有関数にして両方から使う形へ。
  「シングル／ダブルで『各チームN試合』と書かない」を回帰テストで固定。
- `npm test`: 73件すべてPASS。バンドル再生成。
- Phase 8。公開前の機微情報検査で4件を検出。
  (1) `.DS_Store` が追跡されていた (2) progress.md に検証用スプレッドシートID
  (3) findings.md にメールアドレス (4) **13コミットのメッセージに `Claude-Session:` のセッションURL**。
  (1)〜(3) はファイルを修正、(4) は未pushだったため `filter-branch --msg-filter` で該当行のみ除去した。
  `Co-Authored-By` は14件すべて保持。バックアップは `refs/original` に残る。
- `gh repo create bracket-gen --public --source=. --push` で公開。
  https://github.com/harupan119/bracket-gen
- リモート側で再検査し、`Claude-Session` 0件・メールアドレス0件・`.DS_Store` 0件を確認。
- Phase 8完了。
- Phase 9開始。「素朴さ」の正体を特定するため、原本 8team_volleyball_base.xlsx の
  スタイル定義（fonts/fills/borders/cellXfs）を機械抽出した。使用スタイル28種。
  判明した規則: 全内容セルに細いグレー罫線 `#9CA3AF`、表ヘッダは `#4472C4` 地に白の太字10pt、
  セクション見出しは `#DCE6F1` 地に紺 `#1F4E79`、チーム名セルは `#DEEBF7` 地、
  入力欄は `#FFF2CC` 地、補足文は9pt `#6B7280`、フォントは Hiragino Sans、列幅 7/20/5/20/5/26。
  生成側にはこのいずれも無く、それが素朴に見える原因だった。
- `core/theme.js` に配色と役割（title/note/section/tableHeader/team/slot/input/body/label）を定義し、
  payload が役割を `userEnteredFormat` へ写す形にした。
- 3タブの全セルに役割を割り当て。セクション見出しは6列に結合。
- 列幅を実測値に合わせた。当初ブラケットの列幅指定が実物の値を上書きし、説明列が26→18に潰れていた。
  列7以降だけを設定する形へ修正。実物の 7/20/5/20/5/26 という並びは、
  そのまま「偶数列＝チーム名、奇数列＝連結線」のブラケット構成になっている。
- スマホ用と進行表に見出し行の固定を入れた。
- テーマの回帰テストを追加（フォント指定、ヘッダ帯の配色、セクション見出しの配色、
  四辺の罫線、入力欄の黄色地＋TEXT書式、列幅の実測値一致、固定行）。
- `npm test`: 80件すべてPASS。バンドル再生成。
- ループ6: ダブルの敗者側を図にした。勝者側と違い木構造にならない
  （小ラウンドでスロットが半分、大ラウンドでは数が変わらない）ため、行間隔が倍々にならず
  既存の閉じた式が使えない。行位置を反復で追う方式にした:
  小ラウンドでは対になる2つの中点へ寄せ、大ラウンドでは動かさない。
  10チームで 勝者側(行4-37) → 敗者側(38-55) → 決勝 → 決勝R → 最終順位 の構成になる。
- ブラケット頂点のタグの意味を修正。敗者側の頂点（裏⑧の勝者）に「優勝」タグが付いていたが、
  実際は決勝へ進むだけ。`nextMatchOf` を追加し、図の中に親が無ければ模型の `winnerTo` を見る形へ統一。
  結果、ダブルでは勝者側・敗者側の両頂点が「決勝へ進む」タグを持ち、優勝タグは最終順位の1位だけになった。
  シングルではブラケットの頂点がそのまま優勝なので championOf のままで正しい。
- 敗者側の図と頂点タグをテストで固定。`npm test`: 83件すべてPASS。
- google-multi MCP も claude-in-chrome も使えない状態が続いたため、
  **Googleを経由せず見た目を確認する経路**を作った。`tools/preview.mjs` が生成ペイロードの書式を
  そのままHTMLへ描き出し、ヘッドレスChromeでPNG化して目視する。
  payload を読むので生成器の意図と食い違わない。
- 目視で3件の不備を検出。(1) 3位決定戦の「行き先」が空欄（終端試合で winnerTo/loserTo が無い）
  (2) 決定方法が「3位決定戦 3位決定戦の勝者」と重複 (3) 表の中に意味のない空の小箱が並ぶ。
  順に、終端試合は決める順位を書く／ラベルとラウンド名が同じ時は繰り返さない／空セルを結合、で修正。
- `Grid.merge` が書式用の空セルを「値あり」と判定して結合を拒否していたため、実質的な値の有無で見る形へ。
- 2周目の目視で、**不戦勝の通過枠にも箱を描いて同じチーム名が2度並ぶ**問題を検出。
  10チームでは勝者側に6つのシードがあり、`（Aチーム）` が1列目と2列目の両方に出ていた。
  2列目以降で試合を経ていない枠は箱を描かず、枝線だけで通過を示す形へ修正。勝者側・敗者側とも。
- 敗者側の大ラウンドは合流の縦線が無いため最終セルが孤立していた。前の列から横線でつなぐようにした。
- `npm test`: 83件すべてPASS。

## 2026-08-29（続き）
- `google-multi` MCP は落ちていなかった。このMCPは**操作系ツールを discover するまで隠す**仕様で、
  再接続のたびにその状態がリセットされる。`sheets_discover` を呼び直せば使える。確認方法が誤っていた。
- 実シートへ流す前にペイロードの大きさを測ったところ、シングル8チームで152KB・102リクエストあった。
  これは私のcontextを通すには大きく、**Apps Script の実行時間も無駄に食う**ので削った。
  - 全セルが同じ `fontFamily` と `verticalAlignment` を持っていた → シート全体へ `repeatCell` で一括指定。
    `updateCells` の後に、fields を絞って重ねる（絞らないと地色や太字を消してしまう）。
  - 罫線を1セルずつ持っていた → 矩形単位でまとめ、`innerVertical`/`innerHorizontal` で内側を引く。
  - 色成分が19桁の浮動小数だった → 4桁に丸める。罫線だけで色成分が1170個あり、ここが一番効いた。
  結果 152KB/102req → 74KB/128req（リクエスト数は増えたが総量は半減）。
- プレビューも内側罫線に対応させた。外周は範囲の縁だけ、内側はセル間に展開する。
- 目視で意匠が原本の水準に達したことを確認。ブラケット図・格子罫線・ヘッダ帯・セクション見出しが揃った。
- `npm test`: 84件すべてPASS。
- 実シートへ適用。値341セル（2D配列で2.2KB）＋書式105リクエスト（罫線74・地色31）を投入し、
  **Sheets APIが全リクエストをエラーなく受理**した。生成ペイロードが実APIに通ることを確認。
  https://docs.google.com/spreadsheets/d/1LprYpvYhDTqzbmFzZBSA5Xi9-kjToq8S44WKFdyDdAo/edit
- 適用にあたり、値と書式を分けて送ると効率が良いことが分かった。
  値は2D配列（2.2KB）、書式は矩形単位のリクエスト（33KB）。
  `updateCells` に書式を持たせる形だと48KBだったので、分けたほうが軽い。
- ペイロード削減の内訳（シングル8チーム）:
  152KB/102req（当初） → 74KB/128req（共通書式の集約・罫線の矩形化・色の丸め）
  → さらに1セル範囲の内側罫線を省いて縮小。
- 残る未検証は「Sheetsでの実際の見え方」のみ。APIが受理することと、
  HTMLプレビューでの目視は済んでいるが、Sheets自身のレンダリングは別物なので確認が要る。
