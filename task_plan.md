# タスク計画

## 目標
条件（形式・チーム数・コート数）を与えると、トーナメント表・進行表・スマホ用入力欄・自動着色つきの
Google Sheets を生成する仕組みを作る。サークル内で代々引き継げることを最優先とし、
使う側にインストール作業を要求しない。

## 成功条件
- 3形式（シングル / ダブルエリミ / 完全順位決定）を生成できる。
- 生成物の依存関係・順位確定が、全勝敗パターンの機械検証を通る。
- 後輩が「Sheetsをコピー → メニュー → 条件入力」の3ステップだけで使える。
- publicリポジトリに認証情報・スプレッドシートIDを含めない。

## 制約
- チーム数は4〜20を保証。チーム名はA〜Zの自動採番。端数はシード。
- 完全順位決定は 4 / 8 / 16 のみ（2の冪以外はエラーで拒否）。
- 着色は条件付き書式のみ。`onEdit` トリガーは使わない（コピー先で承認プロンプトが出るため）。
- 時刻は出さない。コート数は「枠」のグルーピングにのみ使う。
- 連戦回避は初版では実装しない（差し替え可能なストラテジの受け皿だけ用意する）。

## フェーズ

### Phase 1: 形式3のcore（構造生成と検証）
- [x] リポジトリ骨格と package.json
- [x] 入力バリデーション（2の冪チェック、16チーム警告）
- [x] 完全順位決定の再帰的な構造生成
- [x] 枠割当（依存関係のみ / ストラテジ差し替えの受け皿）
- [x] 全勝敗パターンの機械検証
- **Status:** complete

### Phase 2: レイアウト座標計算
- [x] 2列＝1ラウンド・行間隔倍々の座標式を実装する
- [x] セル二重書き込み・結合セル重なりの衝突検出を実装する
- [x] 4 / 8 / 16 チームで衝突ゼロを確認する
- **Status:** complete

### Phase 3: Sheets生成ペイロード
- [x] 4タブ（トーナメント表／進行表／スマホ用／試合管理[非表示]）のグリッドと数式を組む
- [x] 結果入力をスマホ用の1列に一本化する
- [x] 生成した数式から依存グラフを逆算してモデルと一致することを検証する
- [x] core が `spreadsheets.create` の body と `batchUpdate` のリクエスト配列を出す形にする
- [x] MCP経由で実際に適用し、生成されたSheetsを読み戻して検証する
- **Status:** complete

### Phase 4: 条件付き書式と自動進行
- [x] 試合管理タブの依存関係数式を生成する
- [x] 勝者セル・結果セル・勝ち上がり経路の条件付き書式を生成する
- [x] 同一シート内の隠し補助列を置き、条件付き書式がそこを参照するようにする
- [x] 結果を入力して `effectiveFormat` で着色を確認する
- **Status:** complete

### Phase 5: Apps Script化と配布
- [x] esbuild で core を `apps-script/core.bundle.gs` へバンドルする
- [x] バンドルが素のグローバルスコープで動くことをテストで固定する
- [x] Advanced Sheets Service がシートのコピーで引き継がれるか検証する（★実測で引き継がれることを確認）
- [x] メニューと条件入力ダイアログを作る
- [x] テンプレSheetsで実際に生成し、ブラウザから動作を確認する（初回でレイアウトの不備3件を検出）
- [x] 修正版で再生成し、ブラケット図と枝の罫線が出ることを確認する
- [x] 結果を入力して自動進行が働くことを確認する（①〜⑦まで反映を確認）
- [ ] 進行表の文言修正版で再生成する
- [ ] テンプレSheetsを「リンクを知っている全員がコピー可」で共有する
- **Status:** in progress

### Phase 6: ダブルエリミネーション（10チーム）
- [x] 標準シードブラケット（勝者側）と小/大ラウンド交互の敗者側を実装する
- [x] 決勝リセットを条件付き試合として実装する
- [x] 試合数を実物と突き合わせる（勝者側9・敗者側8・計19で一致）
- [x] 「優勝以外は必ず2敗」の不変量で全チーム数を検証する
- [x] 実シートへ適用し、決勝リセットの分岐を含めて読み戻し検証する
- **Status:** complete

### Phase 7: シングルエリミネーション
- [x] 標準シードブラケット（ダブルの勝者側と同じ構造）で実装する
- [x] 3位決定戦オプションを実装する
- [x] 「優勝は無敗・それ以外はちょうど1敗」の不変量で検証する
- **Status:** complete

### Phase 8: 公開
- [x] README（継承リスクの明記を含む）とLICENSE(MIT)を書く
- [ ] スプレッドシートID・認証情報が含まれないことを確認する
- [ ] `gh repo create bracket-gen --public --source=.`
- **Status:** not started

## エラー記録
| エラー | 試行 | 対応 |
|---|---:|---|
| `npm test` が `Cannot find module '.../test'` | 1 | Node 26 は `--test test/` をモジュールパスと解釈する。`node --test test/*.test.js` へ変更 |
| 最終順位が 7,8,5,6,3,4,1,2 の順で出た | 1 | 試合順のまま並べていた。順位でソートしてから書き出すよう修正 |
| ブラケットが下位帯（5〜8位）から並んだ | 1 | `terminalGroups` を `rankStart` 昇順にソート |
| 未対応の `scoring` が生成の入口を素通りし、レイアウト生成まで遅れて落ちた | 1 | `buildTournament` で `getScoring` を呼び、入口で検証する |
| 実シートで `2-1` の勝者が右チームになった。生値は日付シリアル `46054` | 1 | 結果セルに `numberFormat: TEXT` を指定。ローカルテストでは検出不能で、実適用でのみ露見した |
| 最終順位表のチーム名列が空のまま、ブラケット図も静的テキストだった | 1 | `liveRefFormula` / `controlCell` を共有ヘルパにして、両方を試合管理参照の数式に置換 |
| 条件付き書式の適用が `Invalid ConditionValue.userEnteredValue` で全拒否 | 1 | Sheetsは条件付き書式の数式に他シート参照を許さない。同一シートに隠し補助列 H:I を置いて参照先を局所化 |
| `championOf` タグが3つ付くはずが1つだった | 1 | 置換文字列のエスケープ不一致。行を特定して直接編集 |
| ダブルエリミ導入でレイアウトが `r is not defined` | 1 | ループ変数を `key` に変えた際、本文の `r === 1` が残っていた |
| 総試合数の期待式を `2N` と書いてテストが落ちた | 1 | 正しくは `2N-2 ＋ 決勝R = 2N-1`。テスト側の誤り |
| 並行実行された別エージェントが同リポジトリに重複実装 `core/formats/double.js` / `test/double.test.js` を作成 | 1 | 未追跡のまま温存。`core/index.js` に足された未追跡ファイルへのimportだけ、cloneが壊れるため除去 |
| 決勝Rの勝者がモデルとシートで食い違った（モデルI／シートC） | 1 | シート側が元試合の `$B`/`$C`（当初の左右）を出していた。モデルどおり `$E`（勝者）/`$F`（敗者）へ修正 |
| 上記を既存の依存グラフ逆算テストが見逃した | 1 | 条件式内の `$E$19` を先に拾い、実際に返す値を検査していなかった。条件付き試合を専用テストに分離し、返り値の末尾を検査する形へ |
| esbuild のバナー文字列に入れたバックティックを zsh がコマンド置換として実行し、`npm run build` が再帰的に走ってバナーに npm の出力が埋め込まれた | 1 | バナーからバックティックを除去 |
| バンドルのテストが realm 違いで `deepEqual` に失敗 | 1 | vm コンテキストの値は prototype が別。JSON経由でこちら側へ写してから比較する |
| `matchAll` を `g` フラグなしで呼んで `TypeError` | 1 | 正規表現に `g` を付与 |
| READMEのYAML例が `third_place`、実装は `thirdPlace` で食い違い | 1 | 実装のキャメルケースに統一し、ダイアログのキーをテストで固定 |
| ブラウザ初回生成で、トーナメント表に枝分かれの図が出ず試合リストだけになった | 1 | ブラケット描画が完全順位決定の構造に固定されていた。スロット構造を `tree` として持たせ、任意のシード付きブラケットを図として描けるよう一般化 |
| サブタイトルが「各チーム3試合」と誤表示（シングルでは試合数が一定でない） | 1 | 形式ごとに文言を分けた |
| 見出しが「■ 3位決定戦（3位決定戦〜⑦）」と壊れた | 1 | 決勝と3位決定戦が同じラウンド番号。区切りキーにラウンド名を追加 |
| 枝を結ぶ罫線が無く、名前が列に浮いているだけだった | 1 | `drawBranches` を追加。チーム名セルの下線＝横線、連結列の左罫線＝縦線 |
| 補助列がブラケットの列と衝突（8チームでH列） | 1 | 補助列をブラケット右端の外へ動的に配置する `helperCols` へ変更 |
| 進行表の説明文だけ「各チーム3試合」の誤りが残っていた | 1 | トーナメント表側だけ直していた。`eliminationRule` を共有し、両方から使う |
